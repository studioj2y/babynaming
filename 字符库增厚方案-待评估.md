# 字符库（good_chars.json）增厚方案 · 待评估

> 目的：把「取名引擎」从「玩具级薄库」提到「工具级厚库」。
> 本文只给方向与评估要点，不改动代码。实测数据见末节。

## 一、现状（实测，2026-09-04）

| 项 | 数值 |
|---|---|
| good_chars.json 总字数 | **250** |
| chars.json 底库字数 | 8105（可用水库极大） |
| TAGS_VOCAB 标签数 | **14**（才华/光明/品德/自由/智慧/安宁/灵秀/勇敢/健康/温婉/俊逸/坚韧/仁愛/喜悦） |
| 单标签取字池 | 9～26 字（喜悦 9、温婉 15、智慧 20、光明 25、才华 26） |
| 不选标签时的池 | 250（全集） |

**取字逻辑**（`core.generate`）：`good_chars.json` 中每个字带标签列表 `t`，与用户所选寓意标签做**交集**过滤
（`if tags and not (set(it['t']) & set(tags)): continue`）。

## 二、结论：当前不够厚

1. **标签命中即薄**：用户选 1～2 个具体寓意，命中字仅 9～26 个。生成 12 个候选时字符高度复用，推荐「撞字感」强、惊喜度低。
2. **生产隐患（更重要）**：凡用户所选标签不在 14 词表内（如直觉词「睿/温柔/诗/自然/山水/音乐/艺术」），pool=0 → `generate` 报错 → 前端掉 **mock 占位名**。这是玩具化的最直接故障路径。

## 三、增厚方向（按性价比排序，供评估）

### 方向 1 · 扩充 good_chars.json 字数（主杠杆，必做）
- 现在 250 字 vs 底库 8105，空间巨大。建议扩到 **800～1500** 字。
- 已有现成武器：`data/gen_chars.py` 用 agnes-ai **一次性批量**给 `chars.json` 里的字打标签、写入 `good_chars.json`（构建期，运行时零 AI 成本）。
- 做法：提高 `gen_chars.py` 的采样量/入选阈值，重跑生成扩充后的 `good_chars.json`。
- 影响：直接把每个标签的池从 9～26 提到 50～150+，撞字与 pool=0 风险同步下降。
- 代价：一次性 AI 费用（构建期），loader 无改动（仍读同一个文件）。

### 方向 2 · 标签语义松弛 / 同义扩展（低成本、高收益）
- 用户选「智慧」时，同时纳入「才华/灵秀」相关字；选「温婉」时纳入「安宁/仁愛」等。
- 实现：`TAGS_VOCAB` 旁加一张「标签同义/近义组」映射，过滤时把近义标签并入查询集合（OR 而非单标签）。
- 影响：单查询有效池立刻翻倍，且更贴合用户直觉（选「温柔」也能映射到「温婉」）。
- 代价：加一张映射表 + 过滤处小改，无 AI。

### 方向 3 · pool=0 兜底防退化（防玩具化故障，必做）
- 当过滤后池 < 阈值（如 <8）时，**自动松弛**而非报错：依次丢弃最生僻的标签 → 退回「全 good_chars」→ 仍不足才提示用户放宽，绝不掉 mock。
- 影响：彻底切断「用户选了冷门标签就看到占位名」的失败路径。
- 代价：过滤逻辑小改。

### 方向 4 · 自由期许 → 标签映射强化（配合方向 2）
- 能力 5 `map_free_text_to_tags` 当前把自由文本转标签，若映射弱则 tags 为空 → 退全量（失去个性化）。
- 做法：扩充关键词/近义词典，让「睿/温柔/诗/自然/山水」等直觉词可靠落到词表标签；失败再回退全量（而非空）。
- 代价：词典维护，无 AI（除非接 embedding 相似，见方向 5）。

### 方向 5 · 向量相似取字（长期可选，非必需）
- 用 embedding 把「用户期许语义」直接与字义向量匹配，替代精确标签交集。最灵活但引入依赖与延迟，优先级最低。

## 四、建议落地顺序（若评估通过）
1. 方向 3（防退化兜底）—— 立刻做，零成本堵住生产故障。
2. 方向 1（扩库至 800+）—— 用现成 `gen_chars.py` 重跑，一次性费用。
3. 方向 2 + 4（标签松弛 + 映射强化）—— 顺手做，显著提升体感。
4. 方向 5 视资源再定。

## 五、实测脚本（供复测）
`babyname/test_pool.py`（临时，不提交）：遍历多姓氏×标签×性别，打印 `pool_size` 与标签词频。
复测命令：
`cd babyname && python test_pool.py`

---
## 六、实施进度（2026-09-04 起）

| 方向 | 状态 | 说明 |
|---|---|---|
| 方向 3 · pool=0 兜底防退化 | ✅ 已实施 | `core.generate` 标签筛空时自动退全量好字，返回 `meta.relaxed=True` 与友好提示，**绝不掉 mock** |
| 方向 2 · 标签近义松弛 | ✅ 已实施 | 新增 `TAG_EXPAND` 近义组 + `TAG_ALIASES` 别名归并，`_expand_tags()` 把所选标签扩展到近义/别名集合（OR 查询） |
| 方向 4 · 自由期许映射强化 | ✅ 已实施 | `map_free_text_to_tags` 改为**本地词典优先**（零 AI），直觉词可靠落标签；未命中才走 AI、再失败回退词表 |
| 方向 1 · 扩库至 800+ | ✅ 已实施（HY3 直出，零 AI key） | 用模型自身知识产出约 600 个精选字（带寓意标签+性别+释义），分 5 批经 `data/build_good_chars_hy3_partN.py` 与原有 250 合并、自动去重、剔除底库 `chars.json` 无此字者，最终 **820 字**。全程**不依赖 agnes key**（agnes 此前 HTTP 401 失效，故改用 HY3 直出，更稳）。`good_chars.json` 格式不变（`{c,m,t,g}`），`generate` 无需改 |
| 方向 5 · 向量相似取字 | ⏸ 暂缓 | 用户评估中，暂不实施（详见第七节） |

> 复测（终态，820 字）：预设标签扩展池 智慧 440 / 才华 440 / 灵秀 492 / 仁愛 358 / 安宁 346 / 温婉 277 / 喜悦 207 / 坚韧 179 / 俊逸 177 / 光明 155 / 勇敢 146 / 品德 138 / 自由 112 / 健康 53；线上 `POST /api/generate` 各标签均返回**真名**（`source=None`、非 mock），直觉词（睿/自然/诗/音乐/艺术）经 `TAG_ALIASES` 映射到 223 字池；「霸气」无对应词触发 `relaxed` 退全量 513 字仍出真名。性别分布 M 123 / F 307 / U 390。

## 七、性能与 AI 依赖（每步 AI 调用可能性）

> 评估原则：**取名/观测核心全程本地纯计算，运行时零 AI、零网络**；AI 仅在「个性化层」（自由期许、整盘解读、构建期扩库）条件性出现。

| 步骤 / 能力 | 是否调 AI | 时机 | 调用类型 | 降级 |
|---|---|---|---|---|
| **A线 取名 generate** | ❌ 否 | 运行时 | — | — |
| **B线 观测 analyze** | ❌ 否 | 运行时 | — | — |
| 方向 4 自由期许→标签 | ⚠️ 条件（已最小化） | 运行时，**仅当用户填自由期许** | 本地词典优先（零 AI）；未命中才 1 次 chat | 本地词典命中则不调 AI |
| 能力1 AI 整盘解读 | ⚠️ 条件 | 运行时，**仅当触发解读** | chat (agnes-2.5-flash) | 返回 None→模板 |
| 方向 5 向量相似取字 | ⚠️ 条件（暂缓） | 运行时，**仅当用户填自由期许** | embedding 模型 | 可回退标签/全量 |
| `gen_chars.py` 扩库（方向1） | ✅ 是 | **构建期**（非运行时） | chat（一次性批量） | 不影响线上 |
| 表演台词/立绘/动画/UI | ❌ 否 | — | — | — |

**关键结论**：当前前端运行时 AI 调用次数 = 0（generate 请求 `tags:[]`、`free_text:""` 写死；且前端未调 `/api/ai_review`）。核心取名全程本地、零延迟依赖——这是「工具」最稳的形态。AI 只在接通「个性化层」后才出现，且方向 4 已把最常见的自由期许路径改为本地词典优先，绝大多数情况不需调 AI。

---

*备注：生辰（生肖/八字）功能为后续独立需求，不在此方案范围内；详见工作记忆「babyname 计划功能」。*
